# Delta MVP Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** メニューバー常駐の Delta で、メニューからウィンドウを開き、左右テキストを入力 → 実行 → ユニファイドのカラー diff（行/文字 切替）を表示できる状態にする。

**Architecture:** XcodeGen 生成の macOS アプリ。純粋ロジック `DiffEngine`（`CollectionDifference` ベース、UI 非依存・TDD）と SwiftUI View 層を分離。ウィンドウは AppKit 管理（`NSWindow` + `NSHostingController`）で単一インスタンス保持し、メニューから表示・前面化する（macOS 14 の `Window` Scene は起動時に自動で開くため不採用）。

**Tech Stack:** Swift（言語モード 5）/ SwiftUI / AppKit（ウィンドウ管理）/ Swift Testing / XcodeGen / 外部 SPM 依存なし。

## Global Constraints

- デプロイメントターゲット: **macOS 14.0**
- Swift 言語モード: **5.0**（`SWIFT_VERSION = 5.0`、Swift 6 strict concurrency を回避）
- Bundle Identifier: **`com.capybara.delta`**
- アプリ種別: **`LSUIElement = true`**（Dock 非表示の純メニューバーアプリ）
- ビルド/テストは必ず **`DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer`** を前置（`xcode-select` が CommandLineTools を指すため。sudo・システム設定変更は不可）
- 外部 SPM 依存は追加しない（標準 `CollectionDifference` のみ）
- 細粒度モードは**文字単位（grapheme cluster = Swift `Character`）**。日本語は空白 split が機能しないため空白分割は禁止
- 結果表示はユニファイド（1列）。追加=緑背景 / 削除=赤背景 / 共通=無色
- `.xcodeproj` と生成 `Info.plist` は git 管理しない（`project.yml` が source of truth、`xcodegen generate` で再生成）

---

### Task 1: プロジェクト雛形とビルド（XcodeGen + 最小アプリ + テストターゲット）

`xcodegen` で生成したプロジェクトがビルドでき、テストターゲットが実行できる土台を作る。成果物＝`xcodebuild build` 成功 + スモークテスト緑。

**Files:**
- Create: `.gitignore`
- Create: `project.yml`
- Create: `Delta/DeltaApp.swift`
- Create: `Delta/Views/DiffWindowView.swift`（このタスクではプレースホルダ）
- Create: `DeltaTests/SmokeTests.swift`
- Create: `README.md`

**Interfaces:**
- Consumes: なし
- Produces:
  - `struct DiffWindowView: View`（Task 3 で本実装に置換）
  - スキーム `Delta`（build: `Delta`、test: `DeltaTests`）
  - ビルドコマンド: `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -project Delta.xcodeproj -scheme Delta -configuration Debug -derivedDataPath build build`

- [ ] **Step 1: `xcodegen` を導入**

Run:
```bash
brew install xcodegen && xcodegen --version
```
Expected: バージョン文字列が表示される（例: `Version: 2.x.x`）。既に入っていれば再インストール不要。

- [ ] **Step 2: `.gitignore` を作成**

Create `.gitignore`:
```gitignore
.DS_Store
build/
DerivedData/
*.xcodeproj
Delta/Info.plist
*.xcuserstate
xcuserdata/
.swiftpm/
```

- [ ] **Step 3: `project.yml` を作成**

Create `project.yml`:
```yaml
name: Delta
options:
  bundleIdPrefix: com.capybara
  deploymentTarget:
    macOS: "14.0"
  createIntermediateGroups: true
settings:
  base:
    SWIFT_VERSION: "5.0"
    MARKETING_VERSION: "0.1.0"
    CURRENT_PROJECT_VERSION: "1"
    CODE_SIGNING_REQUIRED: "NO"
    CODE_SIGNING_ALLOWED: "NO"
    CODE_SIGN_IDENTITY: ""
targets:
  Delta:
    type: application
    platform: macOS
    sources:
      - Delta
    info:
      path: Delta/Info.plist
      properties:
        LSUIElement: true
        CFBundleName: Delta
        CFBundleDisplayName: Delta
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.capybara.delta
  DeltaTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - DeltaTests
    dependencies:
      - target: Delta
    settings:
      base:
        BUNDLE_LOADER: "$(TEST_HOST)"
        TEST_HOST: "$(BUILT_PRODUCTS_DIR)/Delta.app/Contents/MacOS/Delta"
schemes:
  Delta:
    build:
      targets:
        Delta: all
        DeltaTests: [test]
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - DeltaTests
```

- [ ] **Step 4: 最小の `DeltaApp.swift` を作成**

Create `Delta/DeltaApp.swift`:
```swift
import SwiftUI

@main
struct DeltaApp: App {
    var body: some Scene {
        MenuBarExtra("Delta", systemImage: "doc.on.doc") {
            Button("Open Diff Window") { /* Task 3 で実装 */ }
            Divider()
            Button("Quit Delta") { NSApplication.shared.terminate(nil) }
                .keyboardShortcut("q")
        }
        .menuBarExtraStyle(.menu)
    }
}
```

- [ ] **Step 5: プレースホルダ `DiffWindowView.swift` を作成**

Create `Delta/Views/DiffWindowView.swift`:
```swift
import SwiftUI

struct DiffWindowView: View {
    var body: some View {
        Text("Delta")
            .frame(width: 400, height: 300)
    }
}
```

- [ ] **Step 6: スモークテストを作成**

Create `DeltaTests/SmokeTests.swift`:
```swift
import Testing
@testable import Delta

struct SmokeTests {
    @Test func testTargetRuns() {
        #expect(true)
    }
}
```

- [ ] **Step 7: `README.md` を作成**

Create `README.md`:
```markdown
# Delta

macOS の常駐型 GUI Diff ツール。メニューバーから小窓を開き、2つのテキストの差分をカラー表示する。

## 必要環境

- macOS 14 (Sonoma) 以降
- Xcode（`/Applications/Xcode.app`）
- [XcodeGen](https://github.com/yonaskolb/XcodeGen)（`brew install xcodegen`）

## セットアップ / ビルド

このリポジトリは `.xcodeproj` を管理していません。`project.yml` から生成します。

```bash
xcodegen generate
```

`xcode-select` が CommandLineTools を指している環境では `DEVELOPER_DIR` を前置してビルドします。

```bash
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer \
  xcodebuild -project Delta.xcodeproj -scheme Delta -configuration Debug \
  -derivedDataPath build build
```

## 実行

```bash
open build/Build/Products/Debug/Delta.app
```

メニューバーの δ アイコン（`doc.on.doc`）から "Open Diff Window" を選ぶとウィンドウが開く。

## テスト

```bash
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer \
  xcodebuild -project Delta.xcodeproj -scheme Delta \
  -destination 'platform=macOS' test
```
```

- [ ] **Step 8: プロジェクトを生成しビルド**

Run:
```bash
xcodegen generate && \
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer \
  xcodebuild -project Delta.xcodeproj -scheme Delta -configuration Debug \
  -derivedDataPath build build
```
Expected: `** BUILD SUCCEEDED **`

- [ ] **Step 9: スモークテストを実行**

Run:
```bash
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer \
  xcodebuild -project Delta.xcodeproj -scheme Delta \
  -destination 'platform=macOS' test 2>&1 | tail -20
```
Expected: `** TEST SUCCEEDED **`（`testTargetRuns` が pass）

- [ ] **Step 10: コミット**

```bash
git add .gitignore project.yml Delta DeltaTests README.md
git commit -m "feat: Delta プロジェクト雛形とビルド土台"
```

---

### Task 2: DiffEngine（行/文字モードの diff、TDD）

UI 非依存の純粋ロジック。`CollectionDifference` でユニファイドなセグメント列を生成する。これが品質の核。テスト先行で実装する。

**Files:**
- Create: `Delta/Models/DiffEngine.swift`
- Create: `DeltaTests/DiffEngineTests.swift`

**Interfaces:**
- Consumes: なし
- Produces:
  - `enum DiffMode: Hashable { case line; case character }`
  - `enum DiffKind: Equatable { case equal; case insert; case delete }`
  - `struct DiffSegment: Equatable { let kind: DiffKind; let text: String }`
  - `enum DiffEngine { static func diff(_ textA: String, _ textB: String, mode: DiffMode) -> [DiffSegment] }`

- [ ] **Step 1: 失敗するテストを書く（行モード）**

Create `DeltaTests/DiffEngineTests.swift`:
```swift
import Testing
@testable import Delta

struct DiffEngineTests {
    @Test func lineEqual() {
        let r = DiffEngine.diff("a\nb", "a\nb", mode: .line)
        #expect(r == [
            DiffSegment(kind: .equal, text: "a"),
            DiffSegment(kind: .equal, text: "b"),
        ])
    }

    @Test func lineInsert() {
        let r = DiffEngine.diff("a", "a\nb", mode: .line)
        #expect(r == [
            DiffSegment(kind: .equal, text: "a"),
            DiffSegment(kind: .insert, text: "b"),
        ])
    }

    @Test func lineDelete() {
        let r = DiffEngine.diff("a\nb", "a", mode: .line)
        #expect(r == [
            DiffSegment(kind: .equal, text: "a"),
            DiffSegment(kind: .delete, text: "b"),
        ])
    }

    @Test func lineReplace() {
        let r = DiffEngine.diff("a\nb", "a\nc", mode: .line)
        #expect(r == [
            DiffSegment(kind: .equal, text: "a"),
            DiffSegment(kind: .delete, text: "b"),
            DiffSegment(kind: .insert, text: "c"),
        ])
    }

    @Test func lineTrailingNewline() {
        // "a\n" -> ["a", ""], "a" -> ["a"]
        let r = DiffEngine.diff("a\n", "a", mode: .line)
        #expect(r == [
            DiffSegment(kind: .equal, text: "a"),
            DiffSegment(kind: .delete, text: ""),
        ])
    }

    @Test func bothEmptyLine() {
        let r = DiffEngine.diff("", "", mode: .line)
        #expect(r == [DiffSegment(kind: .equal, text: "")])
    }
}
```

- [ ] **Step 2: テストが失敗することを確認**

Run:
```bash
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer \
  xcodebuild -project Delta.xcodeproj -scheme Delta \
  -destination 'platform=macOS' test 2>&1 | tail -20
```
Expected: コンパイルエラー（`DiffEngine` / `DiffSegment` 等が未定義）でビルド失敗。

- [ ] **Step 3: DiffEngine を実装**

Create `Delta/Models/DiffEngine.swift`:
```swift
import Foundation

enum DiffMode: Hashable {
    case line
    case character
}

enum DiffKind: Equatable {
    case equal
    case insert
    case delete
}

struct DiffSegment: Equatable {
    let kind: DiffKind
    let text: String
}

enum DiffEngine {
    static func diff(_ textA: String, _ textB: String, mode: DiffMode) -> [DiffSegment] {
        let a = tokenize(textA, mode: mode)
        let b = tokenize(textB, mode: mode)
        return align(a, b)
    }

    /// 行モードは改行を維持するため空サブシーケンスを残す。
    /// 文字モードは書記素クラスタ（Swift Character）単位でトークン化する。
    static func tokenize(_ text: String, mode: DiffMode) -> [String] {
        switch mode {
        case .line:
            return text.split(separator: "\n", omittingEmptySubsequences: false).map(String.init)
        case .character:
            return text.map(String.init)
        }
    }

    /// CollectionDifference の removals/insertions から、
    /// 削除→挿入→共通の順で 1 列のユニファイドなセグメント列を再構成する。
    static func align(_ a: [String], _ b: [String]) -> [DiffSegment] {
        let difference = b.difference(from: a)
        var removedOffsets = Set<Int>()
        var insertedOffsets = Set<Int>()
        for change in difference {
            switch change {
            case let .remove(offset, _, _): removedOffsets.insert(offset)
            case let .insert(offset, _, _): insertedOffsets.insert(offset)
            }
        }

        var segments: [DiffSegment] = []
        var i = 0
        var j = 0
        while i < a.count || j < b.count {
            if i < a.count, removedOffsets.contains(i) {
                segments.append(DiffSegment(kind: .delete, text: a[i]))
                i += 1
            } else if j < b.count, insertedOffsets.contains(j) {
                segments.append(DiffSegment(kind: .insert, text: b[j]))
                j += 1
            } else if i < a.count, j < b.count {
                segments.append(DiffSegment(kind: .equal, text: a[i]))
                i += 1
                j += 1
            } else if i < a.count {
                segments.append(DiffSegment(kind: .delete, text: a[i]))
                i += 1
            } else {
                segments.append(DiffSegment(kind: .insert, text: b[j]))
                j += 1
            }
        }
        return segments
    }
}
```

- [ ] **Step 4: 行モードのテストが通ることを確認**

Run:
```bash
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer \
  xcodebuild -project Delta.xcodeproj -scheme Delta \
  -destination 'platform=macOS' test 2>&1 | tail -20
```
Expected: `** TEST SUCCEEDED **`

- [ ] **Step 5: 文字モード（日本語/絵文字含む）の失敗テストを追加**

Edit `DeltaTests/DiffEngineTests.swift` — `bothEmptyLine()` の後ろ、`struct` の閉じ括弧の前に以下を追加:
```swift
    @Test func japaneseCharReplace() {
        let r = DiffEngine.diff("色赤", "色青", mode: .character)
        #expect(r == [
            DiffSegment(kind: .equal, text: "色"),
            DiffSegment(kind: .delete, text: "赤"),
            DiffSegment(kind: .insert, text: "青"),
        ])
    }

    @Test func japaneseCharInsert() {
        let r = DiffEngine.diff("あい", "あxい", mode: .character)
        #expect(r == [
            DiffSegment(kind: .equal, text: "あ"),
            DiffSegment(kind: .insert, text: "x"),
            DiffSegment(kind: .equal, text: "い"),
        ])
    }

    @Test func emojiGraphemeReplace() {
        let r = DiffEngine.diff("👍", "👎", mode: .character)
        #expect(r == [
            DiffSegment(kind: .delete, text: "👍"),
            DiffSegment(kind: .insert, text: "👎"),
        ])
    }

    @Test func bothEmptyChar() {
        let r = DiffEngine.diff("", "", mode: .character)
        #expect(r == [])
    }

    @Test func largeInputDoesNotCrash() {
        let a = String(repeating: "x\n", count: 2000)
        let b = a + "y"
        let r = DiffEngine.diff(a, b, mode: .line)
        #expect(!r.isEmpty)
    }
```

- [ ] **Step 6: 文字モードのテストが通ることを確認**

Run:
```bash
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer \
  xcodebuild -project Delta.xcodeproj -scheme Delta \
  -destination 'platform=macOS' test 2>&1 | tail -20
```
Expected: `** TEST SUCCEEDED **`（追加 5 件を含め全テスト pass）。`tokenize` が `Character` 単位のため日本語・絵文字が 1 トークンとして扱われる。

- [ ] **Step 7: コミット**

```bash
git add Delta/Models/DiffEngine.swift DeltaTests/DiffEngineTests.swift
git commit -m "feat: DiffEngine（行/文字モードの diff 計算）"
```

---

### Task 3: UI 配線（ウィンドウ管理 + 入力 + 実行 + カラー表示）

メニューから単一ウィンドウを開き、左右入力 → 実行（⌘↵） → ユニファイドのカラー diff を表示する。成果物＝アプリ起動・操作でカラー diff が見える（ビルド成功 + 手動確認）。

**Files:**
- Create: `Delta/Window/DiffWindowManager.swift`
- Modify: `Delta/DeltaApp.swift`
- Modify: `Delta/Views/DiffWindowView.swift`（プレースホルダを置換）
- Create: `Delta/Views/DiffEditorView.swift`
- Create: `Delta/Views/DiffResultView.swift`

**Interfaces:**
- Consumes:
  - `DiffEngine.diff(_:_:mode:) -> [DiffSegment]`、`DiffMode`、`DiffSegment`、`DiffKind`（Task 2）
- Produces:
  - `@MainActor final class DiffWindowManager { static let shared; func show() }`
  - `struct DiffWindowView: View`（@State で textA/textB/mode/result を保持）
  - `struct DiffEditorView: View { @Binding var textA; @Binding var textB }`
  - `struct DiffResultView: View { let segments: [DiffSegment]; let mode: DiffMode }`

- [ ] **Step 1: ウィンドウ管理クラスを作成**

Create `Delta/Window/DiffWindowManager.swift`:
```swift
import AppKit
import SwiftUI

/// 単一インスタンスの diff ウィンドウを保持・表示する。
/// macOS 14 では SwiftUI の `Window` Scene が起動時に自動で開くため、
/// メニューバー常駐アプリとして AppKit で明示的に管理する。
@MainActor
final class DiffWindowManager {
    static let shared = DiffWindowManager()
    private var window: NSWindow?

    private init() {}

    func show() {
        if window == nil {
            let hosting = NSHostingController(rootView: DiffWindowView())
            let newWindow = NSWindow(contentViewController: hosting)
            newWindow.title = "Diff"
            newWindow.styleMask = [.titled, .closable, .miniaturizable, .resizable]
            newWindow.setContentSize(NSSize(width: 540, height: 480))
            newWindow.isReleasedWhenClosed = false
            newWindow.center()
            window = newWindow
        }
        window?.makeKeyAndOrderFront(nil)
        NSApplication.shared.activate(ignoringOtherApps: true)
    }
}
```

- [ ] **Step 2: `DeltaApp` のメニューからウィンドウを開く**

Edit `Delta/DeltaApp.swift` — `Button("Open Diff Window")` の本体を差し替え:
```swift
import SwiftUI

@main
struct DeltaApp: App {
    var body: some Scene {
        MenuBarExtra("Delta", systemImage: "doc.on.doc") {
            Button("Open Diff Window") { DiffWindowManager.shared.show() }
            Divider()
            Button("Quit Delta") { NSApplication.shared.terminate(nil) }
                .keyboardShortcut("q")
        }
        .menuBarExtraStyle(.menu)
    }
}
```

- [ ] **Step 3: 左右入力ビューを作成**

Create `Delta/Views/DiffEditorView.swift`:
```swift
import SwiftUI

struct DiffEditorView: View {
    @Binding var textA: String
    @Binding var textB: String

    var body: some View {
        HStack(spacing: 8) {
            editor("A", text: $textA)
            editor("B", text: $textB)
        }
    }

    private func editor(_ label: String, text: Binding<String>) -> some View {
        VStack(alignment: .leading, spacing: 4) {
            Text(label)
                .font(.caption)
                .foregroundStyle(.secondary)
            TextEditor(text: text)
                .font(.system(.body, design: .monospaced))
                .overlay(
                    RoundedRectangle(cornerRadius: 4)
                        .stroke(Color.secondary.opacity(0.3))
                )
        }
    }
}
```

- [ ] **Step 4: 結果表示ビューを作成**

Create `Delta/Views/DiffResultView.swift`:
```swift
import SwiftUI

struct DiffResultView: View {
    let segments: [DiffSegment]
    let mode: DiffMode

    var body: some View {
        ScrollView {
            Group {
                switch mode {
                case .line:
                    VStack(alignment: .leading, spacing: 0) {
                        ForEach(Array(segments.enumerated()), id: \.offset) { _, segment in
                            Text(segment.text.isEmpty ? " " : segment.text)
                                .font(.system(.body, design: .monospaced))
                                .frame(maxWidth: .infinity, alignment: .leading)
                                .background(background(for: segment.kind))
                        }
                    }
                case .character:
                    Text(attributedText)
                        .font(.system(.body, design: .monospaced))
                        .frame(maxWidth: .infinity, alignment: .leading)
                }
            }
            .padding(4)
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity, alignment: .topLeading)
        .background(Color(nsColor: .textBackgroundColor))
    }

    private func background(for kind: DiffKind) -> Color {
        switch kind {
        case .equal: return .clear
        case .insert: return .green.opacity(0.3)
        case .delete: return .red.opacity(0.3)
        }
    }

    private var attributedText: AttributedString {
        var result = AttributedString()
        for segment in segments {
            var piece = AttributedString(segment.text)
            switch segment.kind {
            case .equal: break
            case .insert: piece.backgroundColor = .green.opacity(0.3)
            case .delete: piece.backgroundColor = .red.opacity(0.3)
            }
            result.append(piece)
        }
        return result
    }
}
```

- [ ] **Step 5: メインウィンドウビューを実装（プレースホルダ置換）**

Replace the entire contents of `Delta/Views/DiffWindowView.swift`:
```swift
import SwiftUI

struct DiffWindowView: View {
    @State private var textA = ""
    @State private var textB = ""
    @State private var mode: DiffMode = .line
    @State private var result: [DiffSegment] = []

    var body: some View {
        VStack(spacing: 8) {
            DiffEditorView(textA: $textA, textB: $textB)
                .frame(minHeight: 160)

            HStack {
                Picker("", selection: $mode) {
                    Text("行").tag(DiffMode.line)
                    Text("文字").tag(DiffMode.character)
                }
                .pickerStyle(.segmented)
                .fixedSize()

                Spacer()

                Button("実行") { run() }
                    .keyboardShortcut(.return, modifiers: .command)
            }

            Divider()

            DiffResultView(segments: result, mode: mode)
        }
        .padding(12)
        .frame(minWidth: 480, minHeight: 420)
    }

    private func run() {
        result = DiffEngine.diff(textA, textB, mode: mode)
    }
}
```

- [ ] **Step 6: ビルド**

Run:
```bash
xcodegen generate && \
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer \
  xcodebuild -project Delta.xcodeproj -scheme Delta -configuration Debug \
  -derivedDataPath build build 2>&1 | tail -20
```
Expected: `** BUILD SUCCEEDED **`

- [ ] **Step 7: 既存テストが引き続き通ることを確認**

Run:
```bash
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer \
  xcodebuild -project Delta.xcodeproj -scheme Delta \
  -destination 'platform=macOS' test 2>&1 | tail -20
```
Expected: `** TEST SUCCEEDED **`

- [ ] **Step 8: 手動で動作確認**

Run:
```bash
open build/Build/Products/Debug/Delta.app
```
確認手順（目視）:
1. メニューバーに δ（`doc.on.doc`）アイコンが出る
2. "Open Diff Window" でウィンドウが前面に開く（起動時に勝手に開かないこと）
3. A に `りんご\nみかん`、B に `りんご\nぶどう` を入力
4. 「行」モードで実行（⌘↵）→ `りんご` 無色、`みかん` 赤背景、`ぶどう` 緑背景
5. 「文字」モードに切替えて実行 → `色赤` vs `色青` 等で 1 文字単位の赤/緑が出る

- [ ] **Step 9: コミット**

```bash
git add Delta/Window Delta/DeltaApp.swift Delta/Views
git commit -m "feat: メニューからウィンドウ表示・入力・カラー diff 表示"
```

---

## 完了の定義

- 全タスクのテストが緑（`** TEST SUCCEEDED **`）
- `xcodebuild build` が成功
- 手動確認（Task 3 Step 8）で行/文字モードのカラー diff が表示される
- スコープ外（ホットキー・設定永続化・履歴・UI 微調整）は次フェーズ
